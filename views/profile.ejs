<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/assets/style.css">
    <script src = "/assets/script.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
    <title>Aim practice</title>
</head>
<body onload="addCanvas()">
    <header>
        <h1 class = "title">Shooting Range</h1>
    </header>
    <div class = "tray">
        <div><h2 class = "score">Score : <span id="points">0</span></h2></div>
        <div><h2 class="timer"> Timer(seconds) : <span id="seconds">60</span></h2></div>
        <div><button class="button" id = "start" onclick="countdown(); targetGenerator()">Start</button></div>
    </div>
    <div id="main">
        <div class="scoreboard">
            <span>
            <p >Top 10 Scores: </p>
            <ol>
                <% for(var i=0; i < topplayer.length; i++){ %>
                    <li><p><%= topplayer[i].name %></p><p id="pt"><%= topplayer[i].score %></p></li>
                <% } %>
            </ol>
            </span>
        </div>
        <canvas id="myCanvas" width="800px" height="480px"></canvas>
        <form id="highscore-user" method="POST" action="/">
            <p>Enter ID to be recorded : </p>
            <div><input class="field" name="name" type="text" placeholder="Only top 10" required><button class="button" id = "submit">Submit</button></div>
        </form>
    </div>
</body>
</html>